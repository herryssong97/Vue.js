function test()
{
    console.log("test  함수"); 
}

console.log( test() );

//자바스크립트 자체내에서 정의가 안된 대상에 부여되는 값이 undefined 이다
//undefined 는 사용자가 부여하면 안되는 값이다. 
//사용자가 객체가 없음을 나타내기 위해 부여하는 값은 null 이다 